# Copy to .env.local for local development, and set the same name as an
# Environment Variable in the admin-panel Vercel project settings.

# The exact URL of the deployed public-site app (no trailing slash), e.g.
# https://galych-agro.vercel.app
# This is where the admin panel sends login/content/upload requests.
NEXT_PUBLIC_API_BASE_URL=
